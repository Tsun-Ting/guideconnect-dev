package com.guideconnect;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main entry point for the GuideConnect application.
 * A web platform connecting tourists with local tour guides.
 */
@SpringBootApplication
public class GuideConnectApplication {

    public static void main(String[] args) {
        SpringApplication.run(GuideConnectApplication.class, args);
    }
}
